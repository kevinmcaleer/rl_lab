"""Optional ROS2 / rviz2 advanced visualization track (runs in Docker / a Linux VM).

Imports here stay light: ``ros2_publisher`` imports ``rclpy`` lazily so the
default install never needs ROS2. See ``docs/getting_started/installation_ros2.md``.
"""
